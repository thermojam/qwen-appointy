# Appointy — Платформа онлайн-записи для мастеров

Современная full-stack платформа для управления записями на услуги бьюти-мастеров. Приложение объединяет мастеров и клиентов в удобном интерфейсе — всё доступно в несколько кликов, без звонков и переписок.

## 📋 Содержание

- [Статус проекта](#статус-проекта)
- [Технологии](#технологии)
- [Структура проекта](#структура-проекта)
- [Быстрый старт](#быстрый-старт)
- [API Endpoints](#api-endpoints)
- [Функциональность](#функциональность)
- [Тестовые данные](#тестовые-данные)

## 📊 Статус проекта

**Версия:** 0.1.0 (MVP в разработке)

### ✅ Реализовано
- ✅ Регистрация и авторизация (JWT access + refresh токены)
- ✅ Role-based доступ (MASTER / CLIENT / ADMIN)
- ✅ Онбординг для мастеров и клиентов
- ✅ Личный кабинет мастера с управлением услугами
- ✅ Личный кабинет клиента с поиском мастеров
- ✅ Система расписания мастеров (график работы, перерывы)
- ✅ Запись на услуги (бронирование с подтверждением)
- ✅ Управление профилем мастера
- ✅ Избранные мастера
- ✅ Система отзывов и рейтингов
- ✅ Уведомления
- ✅ Портфолио работ мастера
- ✅ Дашборд мастера со статистикой
- ✅ Темная/светлая тема (next-themes)
- ✅ Адаптивный дизайн с мобильной версией

### 🚧 В разработке
- ⏳ Расширенная аналитика для мастеров
- ⏳ Календарь записей с drag-and-drop
- ⏳ Чат между мастером и клиентом
- ⏳ Push-уведомления
- ⏳ Интеграция с платежными системами

## 🛠 Технологии

### Frontend
- **Next.js 16** (App Router)
- **React 19**
- **TypeScript**
- **Tailwind CSS 4**
- **React Query (TanStack Query)** — кэширование и управление серверным состоянием
- **Zustand** — легковесный state management
- **class-variance-authority** — вариативные UI компоненты
- **lucide-react** — иконки
- **next-themes** — темизация

### Backend
- **Express.js 5** (TypeScript)
- **Prisma ORM** — типобезопасная работа с БД
- **PostgreSQL** — основная база данных
- **JWT** — аутентификация (access + refresh токены)
- **bcryptjs** — хеширование паролей
- **Zod** — валидация данных
- **dotenv** — управление переменными окружения

### Инструменты разработки
- **tsx** — запуск TypeScript без компиляции
- **concurrently** — параллельный запуск серверов
- **eslint** — линтинг кода

## 📁 Структура проекта

```
qwen-app/
├── prisma/
│   ├── schema.prisma          # Схема базы данных (13 моделей)
│   └── seed.ts                # Сид тестовых данных (7 мастеров, 3 клиента)
├── src/
│   ├── app/                   # Next.js App Router страницы
│   │   ├── book/              # Бронирование услуг
│   │   ├── client/            # Личный кабинет клиента
│   │   ├── dashboard/         # Личный кабинет мастера
│   │   │   ├── appointments/  # Записи мастера
│   │   │   ├── gallery/       # Портфолио
│   │   │   ├── notifications/ # Уведомления
│   │   │   ├── reviews/       # Отзывы
│   │   │   ├── schedule/      # График работы
│   │   │   ├── services/      # Услуги
│   │   │   └── settings/      # Настройки
│   │   ├── masters/           # Профиль мастера
│   │   ├── onboarding/        # Онбординг (master/client)
│   │   ├── search/            # Поиск мастеров
│   │   ├── sign-in/           # Страница входа
│   │   ├── sign-up/           # Страница регистрации
│   │   ├── layout.tsx         # Корневой layout
│   │   └── page.tsx           # Лендинг (главная)
│   ├── features/              # Функциональные модули
│   │   ├── auth/              # Аутентификация
│   │   │   ├── hooks/         # Auth хуки (useAuth, useLogin...)
│   │   │   └── store/         # Zustand store
│   │   ├── dashboard/         # Компоненты дашборда
│   │   ├── favorites/         # Избранное
│   │   ├── onboarding/        # Онбординг
│   │   ├── providers.tsx      # Провайдеры приложения
│   │   └── theme-toggle.tsx   # Переключатель темы
│   ├── server/                # Express backend
│   │   ├── db/                # Prisma клиент
│   │   ├── middleware/        # Auth, errorHandler
│   │   ├── routes/            # API роуты (13 endpoints)
│   │   ├── services/          # Бизнес-логика
│   │   ├── types/             # TypeScript типы
│   │   ├── utils/             # Утилиты
│   │   ├── validators/        # Zod схемы
│   │   ├── app.ts             # Настройка Express
│   │   └── server.ts          # Точка входа
│   └── shared/                # Общие ресурсы
│       ├── api/               # API клиент (axios)
│       ├── lib/               # Общие утилиты
│       ├── types/             # Общие типы
│       └── ui/                # UI компоненты (18 компонентов)
├── middleware.ts              # Next.js middleware (auth, роли)
├── .env                       # Переменные окружения
├── package.json
└── tsconfig.json
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка базы данных

```bash
# Настройте DATABASE_URL в .env
# Пример: postgresql://user:password@localhost:5432/appointy_dev

# Примените схему Prisma
npx prisma migrate dev

# Сгенерируйте Prisma клиент
npx prisma generate

# (Опционально) Заполните тестовыми данными
npm run db:seed
```

### 3. Запуск приложения

```bash
# Development mode (frontend + backend одновременно)
npm run dev

# Только frontend (Next.js)
npm run dev:web

# Только backend (Express)
npm run dev:server
```

Приложение будет доступно:
- **Frontend:** http://localhost:3000
- **Backend API:** http://localhost:3001

### 4. Переменные окружения

Создайте `.env` файл в корне проекта:

```env
# Database
DATABASE_URL=postgresql://user:password@localhost:5432/appointy_dev

# JWT Secrets (измените в production!)
JWT_SECRET=ваш-секретный-ключ
JWT_REFRESH_SECRET=ваш-секретный-ключ-для-refresh

# Server
PORT=3001
```

## 📡 API Endpoints

### Auth (`/api/auth`)
| Метод | Endpoint | Описание |
|-------|----------|----------|
| POST | `/register` | Регистрация нового пользователя |
| POST | `/login` | Вход в систему |
| POST | `/refresh` | Обновление access токена |
| POST | `/logout` | Выход из системы |
| GET | `/me` | Получение текущего пользователя |

### Onboarding (`/api/onboarding`)
| Метод | Endpoint | Описание |
|-------|----------|----------|
| POST | `/master` | Создание профиля мастера |
| POST | `/client` | Создание профиля клиента |
| GET | `/profile` | Получение профиля пользователя |

### Services (`/api/services`)
| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/` | Список услуг мастера |
| POST | `/` | Создание новой услуги |
| PATCH | `/:id` | Обновление услуги |
| DELETE | `/:id` | Удаление услуги |

### Search (`/api/search`)
| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/masters` | Поиск мастеров с фильтрами |
| GET | `/masters/:id` | Профиль конкретного мастера |

### Appointments (`/api/appointments`)
| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/` | Список записей пользователя |
| POST | `/` | Создание новой записи |
| PATCH | `/:id` | Обновление статуса записи |
| DELETE | `/:id` | Отмена записи |

### Schedule (`/api/schedule`)
| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/` | График работы мастера |
| POST | `/` | Добавление рабочего дня |
| PATCH | `/:id` | Обновление графика |
| DELETE | `/:id` | Удаление рабочего дня |

### Dashboard (`/api/dashboard`)
| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/stats` | Статистика мастера (доход, записи, рейтинг) |
| GET | `/recent-appointments` | Последние записи |

### Другие endpoints
- `POST /api/favorites` — Добавить/удалить мастера в избранное
- `GET /api/favorites` — Список избранных мастеров
- `GET /api/notifications` — Уведомления пользователя
- `POST /api/reviews` — Создать отзыв
- `GET /api/reviews/master/:id` — Отзывы о мастере
- `POST /api/portfolio` — Добавить работу в портфолио

## 📦 Функциональность

### Для клиентов
- **Поиск мастеров** — фильтрация по услугам, формату работы, цене, рейтингу
- **Онлайн-запись 24/7** — просмотр свободных окон в реальном времени
- **История записей** — все прошлые и предстоящие визиты
- **Избранное** — сохранение любимых мастеров
- **Отзывы и рейтинги** — оценка качества услуг
- **Напоминания** — уведомления о предстоящих записях

### Для мастеров
- **Управление услугами** — CRUD операций с ценами и длительностью
- **График работы** — настройка рабочих дней, времени и перерывов
- **Управление записями** — подтверждение, отмена, перенос
- **Статистика** — доход, количество записей, средний чек
- **Портфолио** — загрузка фотографий работ
- **Отзывы клиентов** — просмотр и анализ обратной связи
- **Настройки** — управление профилем и параметрами записи

## 🎨 Design System

Проект следует дизайн-спецификации:

- **Шрифты**: Nunito (заголовки), Nunito Sans (текст)
- **Кнопки**: высота 52px, радиус 16px (основные), 8px (поля ввода)
- **Карточки**: радиус 40px + тень
- **Цветовая схема**: поддержка светлой и темной темы
- **Адаптивность**: mobile-first подход

## 📝 Тестовые данные

После выполнения `npm run db:seed` в базе данных создаются:

### Мастера (7 профилей)
| Имя | Специализация | Email |
|-----|--------------|-------|
| Анна Смирнова | Маникюр/педикюр | anna.smirnova@example.com |
| Елена Козлова | Парикмахер | elena.kozlova@example.com |
| Мария Петрова | Визажист | maria.petrova@example.com |
| Ольга Волкова | Брови/ресницы | olga.volkova@example.com |
| Наталья Соколова | Массаж | natalia.sokolova@example.com |
| Ирина Лебедева | Косметолог | irina.lebedeva@example.com |
| Екатерина Морозова | Шугаринг | ekaterina.morozova@example.com |

### Клиенты (3 профиля)
- Татьяна Иванова (client1@example.com)
- Ольга Сидорова (client2@example.com)
- Дарья Кузнецова (client3@example.com)

**Пароль по умолчанию для всех тестовых аккаунтов:** `password123`

## 🔐 Безопасность

- ✅ Пароли хешируются через bcryptjs (salt rounds: 10)
- ✅ JWT access + refresh токены с разным временем жизни
- ✅ Валидация всех входящих данных через Zod
- ✅ Role-based авторизация на уровне middleware
- ✅ Защита от несанкционированного доступа к роутам
- ✅ CORS настроен для безопасного взаимодействия

## 📄 License

MIT
